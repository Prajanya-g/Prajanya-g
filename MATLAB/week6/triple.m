% the function triple the value provided through parameter and returns the
% result
% there is only one parameter the number that needs to be tripled
% The function returns the final result 

function [resultNum] = triple(Num)
% multipling the number by 3
resultNum = Num * 3
end
